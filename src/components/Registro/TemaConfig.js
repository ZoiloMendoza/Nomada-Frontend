import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#E91E63',
    },
    secondary: {
      main: '#2B2E4A',
    },
    terceary: {
      main: '#6C6D7F',
    },
  },
});

export default theme;
